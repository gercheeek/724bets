#!/usr/bin/expect -f
set timeout 30
set ip "85.121.178.80"
set password "kQkrGBYHtY76dIF0z1"

spawn ssh -o StrictHostKeyChecking=no root@$ip "grep -rn 'Big Bass' /var/www/724bahis/ | wc -l"
expect "*?assword:*"
send "$password\r"
expect eof
