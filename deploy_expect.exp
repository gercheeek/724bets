#!/usr/bin/expect -f

set timeout -1
set ip "85.121.178.80"
set password "kQkrGBYHtY76dIF0z1"

spawn scp -o StrictHostKeyChecking=no deploy.tar.gz setup_vps.sh root@$ip:/root/
expect "*?assword:*"
send "$password\r"
expect eof

spawn ssh -o StrictHostKeyChecking=no root@$ip "bash /root/setup_vps.sh"
expect "*?assword:*"
send "$password\r"
expect eof
