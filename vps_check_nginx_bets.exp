#!/usr/bin/expect -f
set timeout -1
set ip "85.121.178.80"
set password "kQkrGBYHtY76dIF0z1"

spawn ssh -o StrictHostKeyChecking=no root@$ip "cat /etc/nginx/sites-enabled/* | grep -E 'server_name|root' -A 1"
expect "*?assword:*"
send "$password\r"
expect eof
