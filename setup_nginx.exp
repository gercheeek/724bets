#!/usr/bin/expect -f
set timeout -1
set ip "85.121.178.80"
set password "kQkrGBYHtY76dIF0z1"

spawn ssh -o StrictHostKeyChecking=no root@$ip "apt-get update && apt-get install -y nginx certbot python3-certbot-nginx && ping -c 1 api.724bahis.net"
expect "*?assword:*"
send "$password\r"
expect eof
