const { Client } = require('ws');
// Wait, actually I can just run server.js with a small modification to log one game.market.
// Let's modify server.js to log one game.market to a file if it has events.
