#!/usr/bin/expect -f
set timeout -1
set ip "85.121.178.80"
set password "kQkrGBYHtY76dIF0z1"

spawn ssh -o StrictHostKeyChecking=no root@$ip "cd /opt/724bets-backend && sqlite3 dev.db 'SELECT id, username, balance FROM User WHERE username=\"Yönetici\";'"
expect "*?assword:*"
send "$password\r"
expect eof
