#!/usr/bin/expect -f

set timeout -1
set ip "85.121.178.80"
set password "kQkrGBYHtY76dIF0z1"

spawn ssh -o StrictHostKeyChecking=no root@$ip "netstat -tlnp | grep 3001"
expect "*?assword:*"
send "$password\r"
expect eof
