#!/usr/bin/expect -f
set timeout -1
set ip "85.121.178.80"
set password "kQkrGBYHtY76dIF0z1"

spawn ssh -o StrictHostKeyChecking=no root@$ip "cat /etc/nginx/sites-enabled/* | grep -E 'root|proxy_pass|location' -A 2 -B 2"
expect "*?assword:*"
send "$password\r"
expect eof
