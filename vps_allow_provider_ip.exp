#!/usr/bin/expect -f
set timeout -1
set ip "85.121.178.80"
set password "kQkrGBYHtY76dIF0z1"

spawn ssh -o StrictHostKeyChecking=no root@$ip "ufw allow from 167.99.33.65 to any && ufw status verbose"
expect "*?assword:*"
send "$password\r"
expect eof
